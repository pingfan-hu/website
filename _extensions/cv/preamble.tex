\usepackage{marvosym} % Allows the use of symbols
\usepackage{parskip} % Spacing adjustments
\usepackage{ragged2e} % Better ragged alignment
\setlength{\RaggedRightParindent}{0pt}

% Global spacing adjustments
\setlength{\parskip}{3pt} % Space between paragraphs
\linespread{0.98} % Line spacing

% Section spacing with underline but no extra vertical space
\RedeclareSectionCommand[
  beforeskip=4pt,
  afterskip=3pt
]{section}
\RedeclareSectionCommand[
  beforeskip=3pt,
  afterskip=2pt
]{subsection}

\makeatletter
\renewcommand{\sectionlinesformat}[4]{%
  #3#4\par\nobreak
  \vspace{-11pt}%
  \rule{\textwidth}{0.5pt}\par\nobreak
  \vspace{0pt}%
}
\makeatother

% Main font

\usepackage{fontspec}
\setmainfont{Optima}[
  Path           = _extensions/cv/fonts/,
  Extension      = .ttc,
  FontFace       = {m}{n}{Font = Optima, FontIndex = 0},
  FontFace       = {b}{n}{Font = Optima, FontIndex = 1},
  FontFace       = {m}{it}{Font = Optima, FontIndex = 2},
  FontFace       = {b}{it}{Font = Optima, FontIndex = 3}
]
\setsansfont{Optima}[
  Path           = _extensions/cv/fonts/,
  Extension      = .ttc,
  FontFace       = {m}{n}{Font = Optima, FontIndex = 0},
  FontFace       = {b}{n}{Font = Optima, FontIndex = 1},
  FontFace       = {m}{it}{Font = Optima, FontIndex = 2},
  FontFace       = {b}{it}{Font = Optima, FontIndex = 3}
]

% Monospace font

\setmonofont{MapleMono-NF}[
  Path           = _extensions/cv/fonts/,
  Extension      = .ttf,
  UprightFont    = *-Regular,
  BoldFont       = *-Bold,
  ItalicFont     = *-Italic,
  BoldItalicFont = *-BoldItalic
]


% Header and footer

\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot[L]{\textcolor{gray}{\footnotesize\emph{Updated \today}}}
\fancyfoot[R]{\textcolor{gray}{\footnotesize\emph{\href{https://pingfanhu.com}{pingfanhu.com}}}}
\fancyfoot[C]{\textcolor{gray}{\footnotesize\emph{Page \thepage\ of \pageref{LastPage}}}}
\renewcommand{\headrulewidth}{0pt} % Removes the header line
\renewcommand{\footrulewidth}{0pt} % Removes the footer line
\usepackage{lastpage} % Required for \pageref{LastPage}

% Links

\usepackage{hyperref}
\usepackage{color}
\definecolor{linkcolour}{rgb}{0,0.2,0.6}
\hypersetup{colorlinks,breaklinks,urlcolor=linkcolour, linkcolor=linkcolour}

% Force left alignment for all content
\raggedright
\let\oldpar\par
\def\par{\oldpar\raggedright}
