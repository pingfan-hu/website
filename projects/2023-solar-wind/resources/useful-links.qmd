```{=html}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/maple-mono@5.2.5/index.min.css">
<style>
.sw-links {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 1em 0;
  justify-content: center;
}
.sw-btn {
  display: inline-block;
  padding: 5px 14px;
  border-radius: 20px;
  border: 1.5px solid #5654A2;
  background: #EEEDF8;
  color: #5654A2;
  font-family: 'Maple Mono', monospace;
  font-size: 0.82em;
  text-decoration: none;
  white-space: nowrap;
  transition: background 0.15s ease, color 0.15s ease;
}
.sw-btn:hover {
  background: #5654A2;
  color: #F8F7F0;
  text-decoration: none;
}
@media (max-width: 520px) {
  .sw-links { flex-direction: column; align-items: center; }
  .sw-btn { min-width: 120px; text-align: center; }
}
</style>
<div class="sw-links">
  <a href="https://rr.pingfanhu.com" class="sw-btn" target="_blank"><i class="bi bi-file-earmark-text"></i> Documentation</a>
</div>
<script>
(function() {
  var btns = Array.from(document.querySelectorAll('.sw-btn'));
  function equalize() {
    btns.forEach(function(b) { b.style.width = ''; });
    if (window.innerWidth <= 520) {
      var maxW = Math.max.apply(null, btns.map(function(b) { return b.offsetWidth; }));
      btns.forEach(function(b) { b.style.width = maxW + 'px'; });
    }
  }
  document.fonts.ready.then(equalize);
  window.addEventListener('resize', equalize);
})();
</script>
```
